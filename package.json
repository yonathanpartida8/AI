{
  "name": "builderynthn-m-beta",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BuilderYNTHN_M-Beta — creador de experiencias románticas digitales",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 8080 || npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}